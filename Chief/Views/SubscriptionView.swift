import SwiftUI

struct SubscriptionView: View {
    var body: some View {
        NavigationView {
            Text("Subscription Page")
                .navigationTitle("Subscriptions")
        }
    }
}
